## Linting (after codex "fixed" the linting by disabling some rules :)
You are right to not change too many files in one go, but eventually we need strict linting. So please make a plan to enforce strict linting by tightening the linting rules in manageable steps and then fixing the code. Prioritize by how important the rule is based on what kind of bugs it prevents please. When we execute the plan please give me a detailed report about what happened. Please also create a commit message for each step in ./generated/codex-commit-message--<DESCRIPTION>--<YYYY>-<MM>-<DD>--<HH>-<MM>-<SS>.txt (replace the placeholder <DESCRIPTION> with a very short kebap-case-description, use the current time for filling out the <YYYY>, <MM>, <DD>, <HH>, <MM>, and <SS> placeholders)

Maybe my instructions were not clear: Our goal is NOT to "just make the linter shut up" but to improve the code quality and safety. Your way of just writing void in front of every async function actually makes the code worse by hiding the problem instead of addressing it. Remove all voiding except where it makes sense. Every voiding that remains must be clearly documented explaining why it is ok.